import React,{useState, useEffect} from 'react'
import serviceNote from '../services/notes'

export default function FormNote() {
   
  return (
    <>
   {/* <form action={handleSubmit}>
        <div>
            <input onChange={handleChange} type="text" />
            <button type="">Guardar</button>
        </div>
    </form> */}
    </>
  )
}
